/* FIB ZAPI parser stub (C1) */
#include <danos/dpa.h>
/* Real implementation parses FRR zebra ZAPI messages.
 * For v0.1 skeleton this is a compile-only stub. */
int danos_zapi_parse_init(void) { return 0; }
