# DANOS-Open v0.16 Acceptance Matrix

更新时间：2026-09-26

本文是 v0.16 当前验收状态的唯一汇总；`docs/project-status.md` 中较早的
路线图和历史阻塞仅用于追溯。

| 项目 | 当前状态 | 证据/边界 |
|---|---|---|
| 构建与确定性测试 | PASS | CTest 34/34 |
| Debian trixie VPP 26.10 | PASS | 源码构建，API/stats socket，DPA conformance |
| FRR 10.3 ZAPI registration | PASS | zserv registration、command 31/32 |
| FRR static route add/withdraw | PASS | command 31/32、DPA sweep、VPP FIB 前后对照 |
| FRR weighted ECMP | PASS | 多 nexthop、VPP load-balance FIB |
| Route/NH/NHGroup deletion | PASS | tombstone、programmed ledger sweep、回归测试 |
| VPP restart replay | PASS | API reconnect、bounded replay、FIB 恢复 |
| FRR daemon/zserv restart recovery | PASS | topology-87 重启 FRR，zserv/BGP peer 恢复；受控 EOF 被识别并完成最终 route restore |
| QEMU e1000 DPDK lane | PASS | 两端口、API/stats、接口 up、FIB、peer ping |
| QEMU VMXNET3 DPDK lane | FAIL（已定位） | QEMU `vmxnet3` 设备可见，VPP 26.10-rc0 在 DPDK 初始化后 SIGSEGV；`pc/q35`、单/双端口均复现 |
| QEMU VMXNET3 native-VPP 对照 | FAIL（中断前置条件） | native `vmxnet3_plugin` 不崩溃，但 QEMU guest 报 `No sufficient interrupt lines (0)`，接口未创建；不作为 DPDK 结果 |
| VMware VMXNET3 DPDK/native 对照 | FAIL（中断前置条件） | VMware Workstation 26.0.0 正确呈现 `0b:00.0`/`13:00.0` 的 `15ad:07b0`，但 native plugin 仍报告 MSI-X interrupt lines 为 0；DPDK 路径同样不能进入流量验收 |
| VMware VMXNET3 DPDK polling-only lane | PASS（双网段报文/packet baseline）/待线速性能 | `no-rx-interrupts` 下两块 `15ad:07b0` 均创建、VPP 接口地址为 `192.168.45.3/24` 与 `192.168.46.3/24`；项目内双 VMXNET3 peer `192.168.45.2/.46.2` 双向 ping 通过；100 包、10ms 间隔基线为约 99.01/98.04 pps、0% loss，VPP 两端口 RX/TX 与 ECMP 两 bucket 均有真实计数；线速 PPS/吞吐仍待专用发生器 |
| QEMU ECMP 多流分布 | PASS（功能）/待增强（性能） | topology-87 通过多流、双路径可达性和 before/after counters；精确 per-bucket 分布与高强度远端流量列入性能 lane |
| BGP/OSPF→DPA→VPP | PASS | topology-87 真实 FRR 10.3 BGP add/withdraw、peer 恢复、OSPF Full、ZAPI 31/32、DPA/VPP 编程全部通过 |
| 真实 PCI DPDK 性能 | ENVIRONMENT-OPEN | QEMU e1000/软件 DPDK lane 已通过；真实 PCI/VMXNET3/SR-IOV 吞吐仍需具备 PCI、HugePages、VPP DPDK runtime 的专用 runner |
| backend contract freeze | PASS | contract 文档、conformance、programming pipeline、复合路由生命周期 gate 已通过 |
| VLAN/VXLAN/EVPN/BFD | DEFERRED | 待 v0.16 验收矩阵关闭后开始 |

## 2026-09-25 增量证据

- `build/qemu-frr-vpp-topology-16` 使用 Debian trixie、FRR 10.3 和当前双端口
  VPP ISO 完成真实长连接验收。
- FRR route-cycle 的 add/withdraw/restore 返回码均为 0，DANOS 串口依次记录
  `command=31`、`command=32`、`command=31`，并对三次事件执行 VPP route add/delete/add。
- 修正 FRR 10.3 route type 订阅为 `4,10`（static、BGP）；FRR 10.3 的
  BGP route type 为 10，9 是 ISIS。
- topology-87 关闭 IPv4、BGP、OSPF、ECMP、多流、FRR daemon/zserv restart 和
  VPP restart replay 的集成 gate；真实 PCI 性能仍不由 QEMU 结果替代。
- topology-26 进一步完成真实 VPP SIGTERM/重启：串口记录
  `VPP-RESTART-TEST PASS`，并在新 API socket 上恢复 `198.51.100.0/24`，
  同时 bridge 日志记录 `replay attempted=1 failed=0`。
- topology-27 的统一 gate 全部通过，新增 `VPP-ECMP-MULTI-FLOW PASS`，并再次通过
  VPP restart replay 与 FRR route lifecycle。
- topology-28 进一步通过真实 FRR 10.3 BGP `End-of-RIB` 和 OSPF
  `Full/DR` 邻接断言；这关闭邻居建立 gate，但 BGP/OSPF 学习路由的独立
  add/withdraw 到 VPP 仍需单独覆盖。

## 2026-09-26 增量证据

- VMware Workstation 双 VM 复测已改为两条独立二层/三层网段，避免把两块 VPP
  接口错误地放进同一 `/24`；VPP 串口明确记录两接口地址、两个 resolved
  邻居和两个 ECMP bucket。
- 项目内 peer VM 使用两块 VMXNET3（内核 `vmxnet3` driver）从
  `192.168.45.2`、`192.168.46.2` 分别 ping DANOS 的 `.3`，两组均为
  3/3、0% loss；DANOS VPP `VPP-DPDK-PING-0/1 PASS`，两端口分别出现
  RX 11、TX 10/16 等真实计数。该结果关闭 VMware polling-only 的“仅初始化”
  门禁，但不等同于高强度性能基线。
- live ISO builder 已固化 `VPP_IF1_ADDR/VPP_IF2_ADDR` 到 guest 环境，并使
  peer 镜像兼容 BusyBox `ip neigh add`；VPP 接口地址冲突会被串口显式保留，
  不再被误报为双端口成功。
- peer ISO 已增加可配置 `DANOS_TRAFFIC_INTERVAL` 及串口 marker
  `PEER-TRAFFIC-BENCH`；本轮 `DANOS_TRAFFIC_COUNT=100`、interval `0.01s`
  双路径均 0% loss，实测约 99.01/98.04 pps。该数值用于重复性和回归趋势，
  不作为 DPDK 线速吞吐结论。
- 1000 包双路径稳定性复测通过：两路径 0% loss、约 97.56/97.47 pps，VPP
  两端口均出现约 1007 RX；该结果纳入 packet-level 长稳趋势，不替代 PCI
  DPDK 线速验收。
- 新增 `danos-test/vmware/verify_vmxnet3_packet_baseline.sh`，自动检查双路径
  marker、地址、VPP counters、0% loss gate 和最低 pps；本轮以
  `VMWARE_MIN_PPS=50` 执行通过。
- 独立 topology-88 使用带 `restart_test=1` 的 Debian trixie/e1000 ISO 重跑，
  统一日志确认 `VPP-RESTART-TEST PASS`、多次 `replay attempted=N failed=0`
  和 API reconnect；该运行的 FRR route-cycle 未完成 withdraw/restore，故不将
  本次运行单独计为完整 FRR 重启 gate，避免把 VPP-only 证据扩大解释。
- 全新 Debian trixie FRR runtime topology-89 已完成统一 verifier：VPP
  restart/replay、BGP add/withdraw、OSPF adjacency、ZAPI add/withdraw、FRR
  route add/withdraw/restore 及 FRR daemon/zserv restart 全部 PASS；manifest
  固定 commit、ISO digest、端口和 topology 路径，QEMU 进程已在验收后关闭。
- VMware Workstation 冷启动复测确认 `danos-open-v0.16.0-rc1-vmware-vmxnet3-polling.iso`
  可从 `Run /init` 继续进入 live system；串口依次出现 VPP API/stats ready、
  `VPP-DPDK-INTERFACES PASS`、双路径 ping PASS、`mgrd: VPP backend connected`
  和 `live system ready`。ISO SHA256 为
  `854298ef956072e31917d4efc663e9f5a31f80cf887cea5f0bdc8d95125587c9`。
- DPDK container lane 已支持 `DPDK_RESULT_FILE` 结构化输出；本宿主当前结果为
  `status=SKIP`（container `danos-vpp-host-1789981447` 不存在），没有将缺失的
  privileged PCI/VFIO runtime 伪造为性能 PASS。

## 验收分层

- L0：单元、wire、并发、fuzz、确定性测试。
- L1：特权 FRR/VPP 控制面及 FIB 编程。
- L2：QEMU/Docker 报文级转发和恢复。
- L3：真实 PCI/VFIO/uio、DPDK 性能和多流分布。

当前 L0/L1 已基本完成，L2 已完成 e1000 与 VMware VMXNET3 polling-only 双端口可达性，L3 受宿主环境
限制；任何 L3 项不得由 QEMU 结果替代。

## 后续执行顺序

1. 固化并版本化 Route/NH/NHGroup contract 与删除/回滚/replay conformance。
2. 增加 FRR、DANOS、VPP 分别重启、zserv 断连、ECMP 路径上下线验收。
3. 固化 QEMU e1000 功能 lane，增加 VMXNET3 和真实 PCI DPDK preflight/perf lane。
4. 建立 PPS、吞吐、延迟、CPU、ECMP bucket 分布和长稳测试基线。
5. 仅在上述基线稳定后进入 BFD、VLAN、VXLAN、EVPN 扩展。
